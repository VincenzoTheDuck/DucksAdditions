const gridstone = extend(Floor, "gridstone", {});
const gridgrass = extend(Floor, "gridgrass", {});
const griddirt = extend(Floor, "griddirt", {});
const gridmud = extend(Floor, "gridmud", {});
const gridsand = extend(Floor, "gridsand", {});
const darkGridsand = extend(Floor, "dark-gridsand", {});
